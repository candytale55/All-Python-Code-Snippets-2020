# Function named average() has two parameters named num1 and num2.
# It returns the average of these two numbers.

def average(num1, num2):
  return (num1 + num2) / 2


print(average(1, 100))
# The average of 1 and 100 is 50.5
print(average(1, -1))
# The average of 1 and -1 is 0
